package com.example.demo.repository;

import com.example.demo.entity.Distr;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DistrRepository extends JpaRepository<Distr, Integer>{
}
